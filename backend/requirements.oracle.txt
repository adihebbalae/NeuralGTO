# NeuralGTO — Oracle Cloud Production Requirements
# ─────────────────────────────────────────────────────────────────────────
# Install on Oracle VM:
#   pip install -r requirements.oracle.txt
#
# This consolidates backend/ (FastAPI) + poker_gpt/ (pipeline) deps.
# Excludes: streamlit (UI-only), dev/test tools (pytest, httpx-dev)
# Python >= 3.13 required (see setup_oracle.sh for install instructions)
# ─────────────────────────────────────────────────────────────────────────

# ── FastAPI backend ──────────────────────────────────────────────────────
fastapi>=0.135.1,<0.136.0
uvicorn[standard]>=0.41.0,<0.42.0
pydantic>=2.12.5,<3.0.0
python-dotenv>=1.2.2,<2.0.0
slowapi>=0.1.9,<0.2.0

# ── poker_gpt pipeline ───────────────────────────────────────────────────
google-genai>=1.0.0          # Gemini API client
rich>=13.0.0                 # CLI output formatting
httpx>=0.27.0                # HTTP client (Ollama / health checks)
