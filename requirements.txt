# PokerGPT Dependencies
# Install with: pip install -r requirements.txt

google-genai>=1.0.0  # Google Gemini API client
python-dotenv>=1.0.0 # Load .env configuration files
streamlit>=1.37.0    # Web UI (>=1.37 required for st.context.headers)
rich>=13.0.0         # CLI output formatting (tables, colors)
httpx>=0.27.0        # HTTP client for local LLM (Ollama) integration
